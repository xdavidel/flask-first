# Flack Web App

First Attempt of Using Flask

### Prerequisites

Make sure that **python3** is installed
install flask via pip

```
pip install flask
```

### Installing

run the main script

```
python app.py
```
